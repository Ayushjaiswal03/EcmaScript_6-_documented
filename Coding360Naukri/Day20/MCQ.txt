Q- What keyword is used to create a constant in JavaScript?

=> const
The const keyword is used to declare a constant in JavaScript, meaning the variable's value cannot be reassigned.


Q- Which CSS property determines how an element's background image is positioned within its container?

=> background-position
The background-position property specifies the initial position of the background image relative to the element. 
It accepts values like keywords (e.g., top, bottom), lengths (e.g., 10px, 50%), or combinations.


Q- Choose the correct option

console.log( 5 + "3" ) ;
console.log( 5 * "3" ) ;

=> 53 , 15


Q- Suppose we have a text "human" that we want to convert into string without using the "new" operator. 
   Which is the correct way from the following to do so in context to JavaScript?

=> Both human.toString( ) and String(human) 
There are three common ways to convert the text into strings:value.toString(),"" + value and String(value). 
We can convert a text to string without using "new" operator that are: human.tostring() and the another one is String(human).


